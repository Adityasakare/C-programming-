#include <stdio.h>

int main()
{
    int a = 5;
    // int b = 7.5; // Not recommended 7.5 is not a interger
    float b = 7.5;
    int d = 5;
    char c = 'a';

    // Declaring varible to store error code

    int error_code;
    printf("THe value of a is %d \n", a);
    printf("THe value of a is %f \n", b);
    printf("THe sum of a and d is %d \n", a+d);
    printf("THe value of a is %c \n", c);
    //'A' --> a charachter

    return 0;
}