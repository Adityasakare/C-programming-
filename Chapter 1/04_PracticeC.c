#include <stdio.h>

int main()
{
    int radius = 3;
    float pi = 3.14;
    printf("THe area of circle is %f\n", pi * radius * radius);
    int height = 5;
    printf("THe volume of cylender is %f\n", pi * radius * radius * height);

    return 0;
}